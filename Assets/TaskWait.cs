using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TaskWait : MonoBehaviour {
    [SerializeField]
    private float tiempoEspera = 5;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public bool WaitTime()
    {
        //Tiempo espera para ejecutar el próximo ciclo
        if ()
        {
            return true;
        }
        return false;
    }
}
